description: |
  The caller is not scoped to that registration.

  | Error code | Description |
  |---|---|
  | NO_RELATIONSHIP | The caller is not scoped to that registration |
headers:
  X-Request-ID:
    $ref: '../headers/response/XRequestID.yaml'
  X-Correlation-ID:
    $ref: '../headers/response/XCorrelationID.yaml'
content:
  application/fhir+json:
    schema:
      $ref: '../schemas/OperationOutcome.yaml'
    example:
      resourceType: OperationOutcome
      issue:
        - severity: error
          code: forbidden
          details:
            coding:
              - code: NO_RELATIONSHIP
          diagnostics: The caller is not scoped to that registration
