name: X-Correlation-ID
in: header
required: false
description: |
  Tracks a transaction across systems, returned unchanged. Need not be
  unique per call. A retry carries the same value.
schema:
  type: string
